apiVersion: v1
kind: Service
metadata:
  name: elec-forecast-api
spec:
  selector:
    app: elec-forecast-api
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080